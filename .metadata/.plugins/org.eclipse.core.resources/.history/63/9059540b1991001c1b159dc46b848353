package br.edu.fatecfranca.ex0;

public class Carro {

	// Declaração de variáveis	
	String modelo;
	int ano;
	float velocidade;
	boolean motor;

	// Métodos construtores
	Carro() {

	}

	Carro(String mod, int a, float v, boolean m) {
		modelo = mod;
		ano = a;
		velocidade = v;
		motor = m;
	}

	// Métodos
	void ligar() {
		motor = true;
	}
	
	void desligar() {
		motor = false;
	}
	
	void acelearar(float x) {
		velocidade += x;
	}

	void frear(float x) {
		velocidade -= x;
	}
	
	void mostrar() {
		System.out.println("Modelo: " + modelo + ", Ano: " + ano + ", Vel: " + velocidade + ", Motor: " + motor);
	}
}